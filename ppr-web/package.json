{
  "name": "ppr-web-workspace",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @ppr/admin dev",
    "build": "pnpm -r build",
    "build:admin": "pnpm --filter @ppr/admin build",
    "preview": "pnpm --filter @ppr/admin preview"
  },
  "devDependencies": {
    "typescript": "^6.0.2"
  },
  "overrides": {
    "minimist": "^1.2.8",
    "node-fetch": "^2.6.7",
    "tmp": "^0.2.4"
  },
  "dependencies": {
    "pnpm": "^10.33.2"
  }
}
